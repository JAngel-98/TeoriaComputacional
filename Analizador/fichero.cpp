#include <conio.h>
#include <stdio.h>
//Programa: Números múltiplos de 4 entre el 64 y el 44
main()
{
    int numero;
    printf(" \n ");
    for ( numero = 44 ; numero != 64 ; numero++ )
    {
    	if(numero%4 == var0)
    	{
    		printf("  %d  ", &numero);
      }

    }
    getch();
    return 0;
}
